# AI Agent 功能使用说明

## 功能概述

在原有AI客服功能基础上，新增了**Agent模式**，用户可以通过AI对话窗口完成**查询航班、选择座位、购买机票**的完整流程，无需切换到其他界面。

## 功能特点

### 1. 模式切换
- **普通模式**：原有的航班信息查询问答功能
- **Agent模式**：可执行实际操作的智能助手
- 通过界面顶部的"切换到Agent模式"按钮进行切换

### 2. Agent模式功能

#### 🔍 航班查询
用户可以通过自然语言查询航班，例如：
- "我想买从北京到上海的机票"
- "查询明天去广州的航班"
- "从深圳到成都"

系统会自动识别：
- 出发城市
- 到达城市
- 出发日期（支持"今天"、"明天"等自然语言）

#### 💺 座位选择
查询到航班后，系统会展示可用航班列表：
```
找到 3 个航班:

[1] CA1234
    北京 → 上海
    起飞: 2025-12-08 08:00
    票价: ¥580.00 | 余票: 120

[2] MU5678
    北京 → 上海
    起飞: 2025-12-08 10:30
    票价: ¥650.00 | 余票: 95
```

用户输入航班编号（如：1）后，系统会提示选择座位：
- 输入具体座位号：1A, 2B, 3C 等
- 输入"随机"让系统自动分配

#### 🎫 购票确认
选择座位后，系统会显示订单确认信息：
```
请确认购票信息:

航班: CA1234
路线: 北京 → 上海
座位: 1A
票价: ¥580.00

回复「确认」购买，或「取消」返回
```

用户回复：
- "确认" 或 "yes" → 完成购票
- "取消" 或 "no" → 取消订单

## 工作流程

```
[用户输入查询] 
    ↓
[解析意图：出发地/目的地/日期]
    ↓
[查询数据库，展示航班列表]
    ↓
[用户选择航班编号]
    ↓
[提示选择座位]
    ↓
[用户输入座位号]
    ↓
[显示确认信息]
    ↓
[用户确认]
    ↓
[执行购票：检查余额→扣费→更新座位→生成订单]
    ↓
[显示购票成功信息]
```

## 技术实现

### 核心类修改

#### ai.h
- 添加 `m_isAgentMode` 标志
- 添加 `m_flightManager` 引用
- 添加 `m_currentUserAccount` 当前用户
- 添加状态机：`Idle` → `WaitingFlight` → `WaitingSeat` → `WaitingConfirm`
- 新增方法：
  - `setFlightManager()` - 设置航班管理器
  - `setCurrentUser()` - 设置当前用户
  - `onModeToggleClicked()` - 模式切换
  - `processAgentResponse()` - 处理Agent响应
  - `parseFlightQuery()` - 解析查询参数
  - `showFlightOptions()` - 显示航班选项
  - `executeBooking()` - 执行购票

#### ai.cpp
- 实现完整的购票流程
- 集成 `DBManager` 进行航班查询和订单创建
- 集成 `MemberSystem` 进行余额检查和扣费
- 添加错误处理和状态回滚机制

#### ai.ui
- 添加模式切换按钮 `btnToggleMode`
- 添加模式状态标签 `lblMode`

#### mainwindow.cpp
- 在 `on_btnAIService_clicked()` 中传递 `FlightManager` 引用
- 传递当前用户账号信息

## 使用示例

### 示例1：完整购票流程
```
用户: 我想买从北京到上海的机票
AI: 找到 3 个航班...
用户: 1
AI: 您选择了航班 CA1234，请选择座位号
用户: 1A
AI: 请确认购票信息...
用户: 确认
AI: 🎉 购票成功！订单号：ORD20251208...
```

### 示例2：查询后取消
```
用户: 查询明天去广州的航班
AI: 找到 2 个航班...
用户: 1
AI: 请选择座位号
用户: 2B
AI: 请确认购票信息...
用户: 取消
AI: 已取消购票，如需重新开始请告诉我。
```

## 安全机制

1. **余额检查**：购票前验证用户飞机币余额
2. **座位验证**：检查座位是否可用
3. **事务回滚**：购票失败时自动恢复座位和余额
4. **状态管理**：通过状态机确保流程正确执行

## 支持的城市列表

目前支持的城市包括：
- 北京、上海、广州、深圳、成都
- 杭州、西安、重庆、南京、武汉

（可在 `parseFlightQuery()` 函数中扩展）

## 注意事项

1. **Agent模式需要登录**：必须先登录系统才能使用购票功能
2. **余额充足**：确保账户飞机币余额足够支付票价
3. **网络连接**：普通模式需要连接到后端AI服务（默认 http://localhost:8000/query_flight，可通过环境变量 `AI_AGENT_API_URL` 指向远程推理服务）
4. **Agent模式离线**：Agent模式的航班查询和购票功能在本地执行，不需要网络连接

## 未来改进方向

1. **智能座位推荐**：根据用户偏好（靠窗/过道）智能推荐座位
2. **多程航班**：支持往返票和中转航班
3. **价格比较**：自动比较不同日期的价格
4. **自然语言增强**：接入更强大的NLP模型提升意图识别准确率
5. **语音交互**：支持语音输入和播报
6. **实时座位图**：显示飞机座位的可视化布局

## 开发者信息

- 实现日期：2025-12-08
- 修改文件：ai.h, ai.cpp, ai.ui, mainwindow.cpp
- 依赖模块：DBManager, MemberSystem, FlightManager, Order

---

如有问题或建议，请联系开发团队。
